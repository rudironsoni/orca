import type { OrcaDistribution } from '../distribution-identity'

// Horca main is a downstream product tree. Official builds come from upstream/main.
export const DOWNSTREAM_DISTRIBUTION: OrcaDistribution = 'horca'
